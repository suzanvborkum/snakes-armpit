{
  "name": "ScrollLoopMenu",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "site.js",
  "scripts": {
    "start": "parcel source/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build source/index.html --no-content-hash --no-minify --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
